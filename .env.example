# VigilCart — Environment Variables
#
# OPENAI_API_KEY is server-side only.
# It is NEVER exposed via NEXT_PUBLIC_ prefixes, client bundles,
# logs, error messages, or the downloadable Test Pack.
#
# Leave blank to run in fixture-only mode (all features work without OpenAI).
OPENAI_API_KEY=
