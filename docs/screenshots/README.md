# Screenshots

The `.png` files here are **placeholders** so the README image links resolve. Replace them with real captures from the running app (`npm run dev`, `http://localhost:3000`):

| File | Capture | Where |
| --- | --- | --- |
| `arena.png` | The naïve-vs-guarded race after clicking **Run the experiment** | Act 2 — The Arena |
| `attack-lab.png` | The before/after panel after **Inject & rerun** (guarded shows "Your edit had zero effect on the outcome") | Act 3 — The Attack Lab |
| `verdict.png` | The Autonomy Score ring, breakdown, and locked approval gate | Act 4 — The Verdict |

Recommended size: 1280×720 or larger. Keep the filenames unchanged so the README links keep working.
